this is the 1st shared file # add by bob

# add by alice
val x = List(1,2,3,4)
val rdd1 = sc.parallelize(x)
rdd1.map(t => t * t)

# add by bob
val y = List(1,3,5,7)
val rdd2 = sc.parallelize(y)
rdd2.map(t => t * 3)

# alice add filter 
rdd1.filter(t => t > 2)

# add by yx
# this is a test for multiple users in the same computer 

# add by yx in yxbranch
val z = List(2,4,6,8,2,6,10)
val rdd3 = sc.parallelize(z)
rdd3.map(t => (t, 1)).reduceByKey((a, b) => a + b)

